# Environment variables
.env
.env.local
.env.production
.env.development

# Firebase service account
*.json
!package.json
!vercel.json

# Node dependencies
node_modules/

# Build outputs
dist/
.build/
.vercel/
.next/

# OS junk
.DS_Store

# Uploads directory
uploads/

# Firebase service account (specific)
firebase-service-account.json

# Firebase config - now committed (web API keys are public, domain-restricted)
# firebase-config.js is now public and safe to commit
